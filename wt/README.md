# wt - Ecoscope Workflow Management CLI

CLI tool for managing ecoscope repositories.

## Installation

Using pixi:

```bash
cd wt
pixi install
pixi run wt --help
```

## Usage

```bash
# Create a new repository from template
wt create
```

## Development

This project uses pixi for environment management. All dependencies are defined in `pyproject.toml`.
